{
  "Environment": "test",
  "CosmosUtility": {
    // In the portal, Under Azure Cosmos DB account (core sql) -> Keys, you can find "URI" & "PRIMARY KEY"
    // CosmosEndpoint = URI, CosmosKey = PRIMARY KEY
    "CosmosEndpoint": "https://abc-xyz.documents.azure.com:443/",
    "CosmosKey": "A-Very-Long-Primary-Key"
  }
}